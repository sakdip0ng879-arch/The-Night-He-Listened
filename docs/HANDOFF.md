# HANDOFF — ส่งงานให้ session ถัดไป

> ★★★★★★★★★★ **สถานะ 2026-08-29 (เซสชันที่ 16): บทที่ 1–9 LOCK หมดแล้ว ·
> บทที่ 10 เขียนครบ 13 ฉาก ตรวจครบ ภาพครบ — รอเคาะ LOCK**
> **build 118 beats · ปี 221 → 262 · ¶1–960 เล่าครบทุกย่อหน้า · วุ่ย 0 เขต — เหลือง่อ 4 เขต**
> ★★ **เริ่มอ่านที่ `LOG.md` หัวข้อ "★★ เริ่มเซสชันหน้าตรงนี้"** — มีกฎแปดข้อที่สะสมมาทั้งโปรเจกต์
>
> **บทที่ 10 — สามอย่างที่จะกัดถ้าไม่รู้:**
> ⓐ **วิทยานิพนธ์ของบท**: ซือหม่าอี้เรียนวิธีจากกวานจงแล้วเอาไปใช้กับเฉาส่วง —
>    ฎีกาปี 241 (c8-14) ที่**เราเขียนขึ้นเอง** กลายเป็นคู่มือรัฐประหารปี 249 ทุกตัวอักษร
> ⓑ **สามระลอกของสี** 258 (3 เขต) · 261 (5) · 262 (9) · **แผ่นดินที่ยอมจำนนใช้ `taken`**
>    ไม่ใช่ `losses` — check_hud ฟ้องทั้งปีถ้ากำลังเกณฑ์เปลี่ยนมือโดยไม่มีใครเสียคน
> ⓒ **`strength` ทุกตัวถูกหนีบใต้ `front[side]`** — ยอด 150k ของจูเก๋อต้าน
>    จึงต้องเขียนเป็นตัวหนังสือในป้าย (แบบเดียวกับหมุด 160k ที่ c9-06)
> ⓓ **ปิดหนี้ ▣ ฝั่งตะวันออกแล้ว** — node ใหม่แค่ 3 จุด (เย่ · ฟ่านหยาง · เจี้ยนเย่)
>    · `fanyang` สอบพิกเซลแล้ว → (1117,140) ถอด chk (โรคพิกัดผิดตัวที่หก)
>    ⚠ **หนี้ก้อนใหญ่ที่สุดเหลืออยู่ที่บทที่ 11** (หกทัพปี 269 — hefei · xiangyang ·
>      jiangling · xiakou · yongan) — **วางแผนกราฟก่อนเขียนฉาก**
> บทที่ 9 เขียนครบ 11 ฉาก ตรวจครบ ภาพครบ — รอเคาะ LOCK**
> **build 105 beats · ¶1–816 ของต้นฉบับเล่าครบทุกย่อหน้า · เหลือภาคสิบกับสิบเอ็ด**
> ★★ **2026-08-29 เจ้าของทักบทที่ 8–9 สองข้อ แก้แล้วทั้งคู่** (LOG §5 เซสชันที่ 16):
> ⓐ หมุดทัพสนามฮั่นย้ายจากเฉินชาง → **อู่จ้างหยวน** (ทัพสนามนั่งริมถนนที่ศัตรูเดินผ่าน
>    อ่านเป็น "เชิญเข้ามา") + **คำตอบสามชั้นว่าทำไมซือหม่าอี้ไม่ตีค่ายนา** (ปฏิทินข้าว
>    เกี่ยวช้ากว่าเส้นตายเขาหนึ่งเดือน · เอกสาร 232 ห้ามค่ายนาเก็บข้าวเกิน 30 วัน ·
>    ล้อม = หยุดเดิน) — ต่อยอดจาก c8-04 ซึ่งตัดทั้งสองทาง
> ⓑ ★★★ **ค่าผ่านทางเฉินชาง — plot hole ที่ใหญ่ที่สุดของบทที่ 8** (เจ้าของจับ):
>    เส้นเสบียงของซือหม่าอี้วิ่งผ่าน node เฉินชางพอดี ทุกเกวียนต้องผ่านหน้ากำแพงที่มี
>    ฮั่น 8k อยู่ข้างใน — เดินผ่านเฉย ๆ ไม่ได้ · แก้ด้วย **กองปิดล้อม**: 240 ทิ้ง 15k
>    (60k→45k) · 241 ทิ้ง 20k (80k→60k) · **เฉินชางไม่ได้ถูกล้อม มันแค่ถูกเฝ้า**
>    เพราะประตูใต้ (ด่านซ่าน→ฮั่นจง) กับประตูตะวันตก (หุบเว่ย→หลงโย่ว) ยังเปิด
>    ⚠ **ทางแก้ที่เจ้าของเสนอตอนแรก (เทียนสุ่ยไม่ต้องเสีย) ชน ¶638/¶658 ตรง ๆ** —
>      ต้นฉบับเขียนชัดว่าเขายึดเทียนสุ่ยได้สองครั้งในบทนี้ · ท้วงแล้วและเจ้าของเคาะทางใหม่
>    ★ ขนาดกองปิดล้อมถูกกำหนดโดย 34k ที่อู่จ้างหยวน (เล็กกว่านี้ถูกกวาดทิ้งในสองวัน)
>      — **ประตูกับค่ายนาเป็นเครื่องเดียวที่มีสองชิ้น**
> ⓒ **ฉากใหม่ c9-02 "ปีที่แอ่งเลิกเป็นทางผ่าน" (242)** — ปิดช่องว่าง 234–242 ที่ฎีกา
>    ย้ายราชธานีอ้างเงื่อนไขซึ่งยังไม่เคยถูกจ่าย · สามประตูได้กองประจำ 18k จากหลงโย่ว ·
>    ค่ายนาขยายตะวันออก + ครัวเรือนเชียง/ตี้ · ปูตลาดนัดของบทส่งท้าย
> ★★ **เริ่มอ่านที่ `LOG.md` หัวข้อ "★★ เริ่มเซสชันหน้าตรงนี้"** — มีกฎหกข้อที่เพิ่งเกิด
>
> **สองอย่างที่เซสชันที่ 15 เปลี่ยนและจะกัดถ้าไม่รู้:**
> ⓐ ★★ **ธงพลิกตอนทัพเดินถึง ไม่ใช่ตอนเข้าฉาก** (เจ้าของสั่ง · DECISIONS §7) —
>    `setOwners(owners, hold)` พักการทาสีเขตที่ `mapDelta` ระบุไว้ ให้ `setMarkers`
>    ปล่อยเมื่อลูกศรเส้นสุดท้าย settle · ฉากที่ไม่มีลูกศร/scrub/init/`shot.ps1` พลิกทันที
>    ⚠ **ตัวตรวจทุกตัวเป็น static จึงไม่มีทางเห็นข้อนี้** ทดสอบได้ทางเดียวคือเปิดของจริง
>      และแท็บที่ไม่ได้อยู่หน้าจอมี `visibilityState:"hidden"` ซึ่งหยุด rAF → tween ไม่มีวันจบ
> ⓑ ⚠ **ตัวตรวจที่ throw ตั้งแต่โหลดจะดู "เขียว" ถ้านับแต่บรรทัด `✖`** — เกิดขึ้นสองรอบ
>    ในเซสชันนี้จากการแก้ตาราง CLAIMS ด้วยสคริปต์แล้ว comma หาย · แตะไฟล์ตัวตรวจเมื่อไร
>    รัน `node --check` ทันที
>
> **★ บทที่ 9 (ชะง่อน) — สิ่งที่ต่างจากทุกบทที่ผ่านมา:** มันเล่า *เจ็ดปีที่ไม่มีสงคราม*
> ทั้งบทจึงมี `mapDelta` **ครั้งเดียวและอยู่ที่ฉากแรก** · ศึกปี 244 ไม่มีการรบสักครั้ง
> จนถึงวันถอย · และแสนหนึ่งของเฉาส่วงคือ **การทุบเพดานที่ c8-05 คำนวณไว้ ต่อหน้าคนอ่าน**
> (เสมียนคำนวณว่าถนนเลี้ยง *ทั้งแนวรบ* ได้แสนเดียว เขาใส่แสนหนึ่งเข้าไปในชะง่อนก้อนเดียว)
>
> **★★ เจ้าของอ่านบทที่ 8 แล้วทักสองข้อ — แก้แล้วทั้งคู่ (รายละเอียด LOG §5 เซสชันที่ 14 ข้อ 5):**
> ⓐ *"ฉากห้ากอง — ทัพหลักมาไม่ถึงเฉินชาง ดันมีทัพแยกออกซะได้ เคยเตือนเรื่องลำดับลูกศรแล้ว"*
>    → **หนึ่งเฟรมไม่มีเวลา** · แตก c8-06 เป็นสองฉาก (ท่อนเดินทาง / ท่อนห้าสาย) แบบ c7-04→c7-05
>    · ห้าสายแยกจาก**หมุดทองที่เฉินชาง**ซึ่งฉากก่อนหน้าเพิ่งพาขบวนไปวางไว้
>    ⚠ **ตัวตรวจไม่มีวันจับข้อนี้ให้** — check_map ยกโทษด้วยกฎ "splits off a column
>      already on the board" ซึ่งจริงเชิงข้อมูล แต่ไม่จริงเชิงเวลา
>    ★ พลอยปิดหนี้ §12 ขั้น 1.6: 238 เคยเป็นการทัพเดียวในบทที่มีฉากเดียว —
>      **น้ำหนักต้นฉบับกำหนดว่าฉากไหน*ยาว* ไม่ใช่ว่าการทัพไหนได้*กี่ฉาก***
> ⓑ *"เว่ยไม่คิดยึดอู่จ้างหยวนไว้ปลูกข้าวเองเลยเหรอ พลอตโฮลหรือเราอ่านพลาด"* →
>    **พลอตโฮลจริง** · คำตอบสามข้อ (ค่ายนาคืออำเภอไม่ใช่ทหารถือจอบ — วุ่ยขนคนพวกนั้น
>    ออกไปเอง 234 · คลองสมัยฉินตายเพราะไม่มีใครลอกสองปี · ค่ายนาถอยไม่ได้ และมันจะ
>    ไปนั่งใกล้ม้าสองหมื่นที่ไม่ต้องขออนุมัติ) + หมัดปิด *"ข้าทำสำเร็จมาแล้วสี่ปีที่หวยหนาน
>    เพราะที่นั่นมีแม่น้ำ มีคน ไม่มีศัตรูข้างหลัง — ที่นี่ไม่มีสักข้อ"*
>    · ยกขึ้นที่ **c8-04** (+หมุดทองอู่จ้างหยวน) ปิดเป็นทางการที่ **ฎีกา c8-14**
>    → ฎีกาปิดคำถามชัด ๆ **สองข้อ**แล้ว: ทำไมไม่เสริมฉางอาน + ทำไมไม่ตั้งค่ายนา
> ★★ **เริ่มอ่านที่ `LOG.md` หัวข้อ "★★ เริ่มเซสชันหน้าตรงนี้"** (ท้าย §5)
>
> **สิ่งที่เซสชันนี้ทำ — ปิดคำทักของเจ้าของหนึ่งข้อ แล้วเขียนบทที่ 8 ให้จบ:**
> ① ★★★ เจ้าของทัก *"ฉากแนวเหนือที่หายไปทั้งเส้น จู่ ๆ เว่ยมีทหารพุ่งมาสามหมื่น
>    ไม่มีเส้นว่ามาจากตรงไหน ส่งมาจากนครหลวงไหม"* — คำทักถูก · c8-05 เดิม*ตั้งใจ*
>    ไม่มีลูกศร ผลคือวุ่ย +25k บน HUD โดยไม่มีอะไรบนกระดานอธิบาย
>    → **กฎใหม่ทั้งเล่ม (DECISIONS §9): `front` ขึ้น = ต้องมีเส้นบนกระดาน**
>      พร้อม `strength` **และ** `who`/`name` (ไม่มีชื่อ = engine ไม่วาดชิป = ไม่มีเลขบนจอ)
>      · Σ strength ของลูกศรที่เข้าแนว = delta ของ `front` เป๊ะ
>    → **คำตอบคือไม่ใช่นครหลวง** — กองชายแดนเหนือที่เลิกมีชายแดนให้เฝ้า (ผลของ c8-01)
>      เดินลงหุบเฟิน `จิ้นหยาง→ผิงหยาง→เหวินสี่→เหอตง→ท่าเหมาจิน→หลังด่านหานกู่`
>      **ไม่ผ่านลั่วหยางสักก้าว** · 34k แยกเป็น 9k เฝ้าป้อมต่อ + **25k เดินลงใต้** (65+25=90 พอดี)
> ② **node/edge ใหม่ 5+5** — `jinyang` `pingyang` `wenxi` `hedong` `maojin`(ford) ·
>    ลาก `d` จริงทุกเส้นเลาะฝั่งตะวันออกของลำน้ำเฟิน · ถนนสายนี้จะได้ใช้อีกภาคสิบ–สิบเอ็ด
> ③ ★ **`jinyang` พิกัดผิดแบบเดิม (ตัวที่ห้า)** — (848,358) อยู่บนลำน้ำข้างตัวอักษร
>    ไม่ใช่ dot · อ่านพิกเซลได้ **(863,344)** ถอด `chk` แล้ว
>    (โรคเดียวกับ wudu · xiabian · lueyang · mumen — เจอด้วยวิธีเดียวกันทุกตัว)
> ④ **c8-06…c8-13 เขียนครบ** · เทียนสุ่ยพลิกสามครั้งในบทเดียว และ §7 (ห้าม clash+delta
>    เมืองเดียวกันฉากเดียวกัน) บังคับให้หาที่ชนใหม่ทุกครั้ง — คำตอบตรงกับต้นฉบับพอดี:
>    **ชนที่เจียถิง**เมื่อวุ่ยยึด (ฝั่งที่วุ่ยเข้ามา) · **ชนที่มู่เหมิน**เมื่อฮั่นชิงคืน (สันที่ต้องไต่)
> ⑤ ⚠ **`label` บน `clash` ไม่ถูกวาดลงจอเลยทั้งโปรเจกต์** — engine บังคับชื่อเมืองบนแผ่น
>    ให้โผล่แทน (จงใจ · `js/strategic.js` ~813) · label พวกนั้นเป็นเอกสารในซอร์สเท่านั้น
>    **อย่าฝากข้อมูลที่คนอ่านต้องเห็นไว้ในนั้น** (BUGS_SEEN §E13)
> ⑥ ⚠ `check_map` เทียบชื่อปลายลูกศรกับ `label` **ตรงตัว** — ต้องเขียน "ด่านหานกู่"
>    ไม่ใช่ "หานกู่" มิฉะนั้นฟ้องเป็น error (หรือปักหมุดที่ปลายทางแทน)
>
> **สามอย่างที่ล็อกไว้ทั้งบทที่ 8 — ห้ามเปลี่ยนถ้าเปิดกลับมาแก้:**
> ① กวานจงไม่พลิกสักครั้ง ทั้งที่วุ่ยเดินผ่านสี่หน — ธงพลิกเฉพาะเทียนสุ่ย
> ② แปดหมื่นปี 241 มาจากการลอบสังหารเคอปี่เหนิงปี 235 (โซ่ c8-01→c8-05→c8-11)
> ③ เพดานแสนเดียวของชะง่อนหานกู่ — 110k = เกินเพดาน จึงถือได้ห้าเดือน ·
>    ตั้งไคลแมกซ์ภาคเก้าไว้แล้ว (เฉาส่างพาแสนคนเข้าชะง่อนเดียวกัน)
>
> `front` ไล่ทั้งบท: 50 → 65 → **90** → 88 → 95 → 80 → **110** → 108
> (ฮั่น 80 → 79 → 77 → 67 → 78 → 75) · ทุกครั้งที่ขึ้นมีลูกศรรองรับแล้ว
> ★★★★★★ **(เซสชันที่ 13 รอบสาม): หลอด HUD แนวตะวันตกทำเสร็จแล้ว**
> **เจ้าของเคาะโครงบทที่ 8 ไว้แล้วสามข้อ (2026-08-27) — เริ่มได้เลย:**
> ① **13 ฉาก** แตกตามน้ำหนักต้นฉบับ: การทัพ 236 = 2 ฉาก · 238 = 1 · 240 = 2
>    (บังคับ — สองธงเปลี่ยนมือ §7 ห้าม clash+delta เมืองเดียวกันฉากเดียวกัน) · 241 = 3
>    + อีก 5 ฉากที่เหลือ (เคอปี่เหนิง · ซือหม่าอี้กลับ · เอกสารนายกอง · เฉารุ่ยสิ้น · ฎีกา)
> ② **ย้ายเฉารุ่ย (239) ขึ้นก่อนบทสรุป 236–241** — ปีเดินหน้าตลอด ไม่มีคำเตือน
>    และบทสรุปกลายเป็นสะพานเข้าไคลแมกซ์ปี 241 พอดี
> ③ ★ **ฟิลด์ `front` ใหม่ใช้ได้แล้ว** — บทที่ 8 ต้องประกาศทุกครั้งที่ดุลแนวตะวันตกเปลี่ยน
>    (ยอดปิดบทที่ 7 คือ ฮั่น 80k · วุ่ย 50k) · ⚠ **ทัพแปดหมื่นของซือหม่าอี้ปี 241 ต้องมี
>    ที่มาบนกระดาน** — วุ่ยเหลือแนวตะวันตก 50k แปลว่าต้องโยกทัพกลางมาอีก 30k+ ระหว่าง
>    236–241 ซึ่งต้องเป็นเหตุการณ์ ไม่ใช่ตัวเลขที่โผล่มาเฉย ๆ (บทเรียนตรงจาก c7-11)
>    · CLAIMS มีแถวรออยู่แล้ว: `[null,"wei",8,241,"เขาเข้ามาในเดือนที่สาม กำลังพลแปดหมื่น"]`
> ⚠ **บทที่ 8 มีกับดักที่รู้ล่วงหน้าแล้ว** — STORYBOARD วาง c8-03/04/05 เป็น **สามการทัพ
> ในสามฉากเดี่ยว** ซึ่งเข้าข่าย "รวบรัด" แบบเดียวกับที่เจ้าของทักในบทที่ 7 เป๊ะ ๆ
> **ให้แตกตั้งแต่ตอนร่าง beat ไม่ใช่แก้ทีหลัง** (§12 ขั้น 1.5 บัญชีทัพ + 1.6 beat ตามเหตุการณ์)
> เพิ่มอีกสองบทเรียนจากบทที่ 7 ที่ต้องเอาไปใช้ตั้งแต่ต้น:
> · **ทุกก้อนในบัญชีทัพต้องมีที่ยืนบนจออย่างน้อยหนึ่งฉาก** (ไม่งั้นได้ "8,500 ที่ไม่มีใครเห็น")
> · **เมืองที่ยอมจำนนต้องมี `clash`+`losses` ที่ฉากก่อนหน้า และ `taken` ที่ฉาก delta**
>   ไม่งั้นแผ่นดินเปลี่ยนมือโดยไม่มีราคา = อาการ "เว่ยแพ้ง่าย"
>
> ★★★★★ **อัปเดต 2026-08-27 (เซสชันที่ 13) — `LOG.md` §5 เซสชันที่ 13**
> เจ้าของสั่งเปิดบทที่ 6 (LOCK) แก้ c6-14 + สั่งแก้ "แถบกำลังพลไม่เคยขยับ"
> **สี่อย่างที่เปลี่ยนแล้วจะกัดถ้าไม่รู้:**
> ① **`mumen` ย้าย (311,636) → (339,641)** — ของเดิมอยู่บนที่ว่างข้างตัวอักษร (โรคเดียวกับ
>    wudu/xiabian/lueyang **ตัวที่สี่**) · d ของ tianshui–mumen · mumen–qishan · weigu–mumen
>    ลากใหม่หมด + edge ใหม่ `mumen–jicheng` · `han_retreat_231` ไม่ผ่าน qishan แล้ว
> ② ★★ **`strength()` ปัดที่หลักพัน ไม่ใช่หลักหมื่น** — นี่คือสาเหตุจริงของ "ตัวเลขไม่เคยขยับ"
>    ค่าจริงขยับทุกปีอยู่แล้ว การปัดเป็นคนกลืนมันทิ้ง · **อย่าเปลี่ยนกลับ**
> ③ ★★ **ฟิลด์ใหม่ `taken`** (หน่วยหมื่น เหมือน `losses` แต่ **ไม่มี RECOVER**) = กองที่ยอมจำนน
>    ใช้ที่ c7-06/c7-12 · `build_timeline` บังคับว่าฉากต้องมีหมุดของฝ่ายนั้นที่มี `strength`
> ④ **HUD เขียนทหารเป็น `431k` ไม่ใช่ `431,000`** — ⚠ §2.5 ข้างล่างเคยเจ็บเรื่องหน่วยนี้มาแล้ว
>    ทุกครั้งที่แตะบรรทัดนั้นใน `ui.js` **ต้องถ่ายภาพดู** ตัวตรวจไม่เห็นหน่วยที่หายไป
> · ของค้างข้อ ⑤ ของเซสชันที่ 12 (หางกระดก c6-14) **ปิดแล้ว**
>
> ★★★★ **อัปเดต 2026-08-27 (เซสชันที่ 12) — `LOG.md` §5 เซสชันที่ 12**
> เจ้าของรีวิวบทที่ 6–7 แล้วทักสามเรื่อง (ทางคด · ลำดับเส้นบทที่ 7 · เว่ยแพ้ง่าย)
> **เคาะให้แก้ทั้งสิบข้อ + สั่งตรวจ San Pass เพิ่ม** ทำครบแล้วทุกข้อ · ตัวตรวจ 5/5 เขียว
> ภาพจริงดูครบ 8 ใบ · **บทที่ 7 ยังรอเคาะ LOCK เหมือนเดิม**
> สิ่งที่เปลี่ยนซึ่งจะกัดถ้าไม่รู้:
> ① **`hanzhong–sanpass` ถูกถอนทิ้ง** — ถนนเฉินชางของจริง (กู้เต้า) คือ ฮั่นจง → หยางผิง →
>    **เลว่หยาง** → ด่านซ่าน → เฉินชาง รวม 720 ลี้ · เส้นเดิมลากทับบล็อก "Mount. Chen Cang"
>    ที่แผ่นพิมพ์ไว้ (พิสูจน์ด้วยการสุ่มพิกเซล) **ห้ามลากกลับมา**
> ② **`lueyang` ย้ายพิกัด (388,743) → (407,758)** — ของเดิมเป็นกึ่งกลางกรอบป้าย ไม่ใช่ dot
>    (โรคเดียวกับ wudu/xiabian) · **ข้อยกเว้น §16 เรื่องเลว่หยางเลิกแล้ว** เหลือ weinan ข้อเดียว
> ③ **edge ใหม่สองเส้น**: `jicheng–tianshui` · `chencang–wuzhang` — reroute ไปแล้วหกการเดินทัพ
> ④ **ตัวตรวจเปลี่ยนสองที่**: `check_map` ข้อ 5 ยกโทษเป็น**รายลูกศร**แล้ว (ไม่ใช่รายฉาก) ·
>    `check_routes` มี**ข้อ 12 ใหม่** วัด "ท่อนที่พาทัพถอย" — ⚠ อย่าเปลี่ยนกลับไปใช้
>    detour ratio หรือมุมหัก ลองแล้วมันฟ้องถนนที่ถูกต้องเกือบทุกเส้น (เหตุผลใน DECISIONS §4)
> ⑤ **ของค้างที่ตัวตรวจเพิ่งเริ่มฟ้อง — รอเจ้าของเคาะ**: `han_retreat_231` ท่อน
>    qishan→jicheng ถอย 26 หน่วย (22%) = หางกระดกของ c6-14/15 · แก้ได้ด้วย edge
>    `mumen–jicheng` แต่ **บทที่ 6 LOCK แล้ว** จึงยังไม่แตะ
>
> ★★★ **อัปเดต 2026-08-27 (เซสชันที่ 11): เนื้อหาส่วนใหญ่ของไฟล์นี้เก่ากว่าสถานะจริงมาก**
> — สถานะปัจจุบัน: **บทที่ 1–6 LOCK · บทที่ 7 แตกใหม่เป็น 15 ฉากแล้ว (รวม 79 beat)
> เขียนครบ ตรวจครบ ภาพครบ — รอเจ้าของดูภาพ/อ่าน แล้วเคาะ LOCK**
> **จุดเริ่มเซสชันหน้าอยู่ที่ `LOG.md` §5 เซสชันที่ 11** — ทักสองเรื่องของเซสชันที่ 10
> ปิดแล้วทั้งคู่: ① "รวบรัด" → แตก 4 การทัพเป็น 8 ฉาก (เจ้าของเคาะโครง 15 + ◆ สี่ข้อ
> 2026-08-27) ② "ทหารเว่ยดูโคตรน้อย" → มวลวุ่ยมีเลขบนหมุดทุกฉากรบ · เป่ยหยวน 20k
> ตาม ¶540 · กองหนุนเฉินชาง 25k ยกออกจริง (◆3 แบบ ก) · โทนที่เจ้าของวาง:
> "สมเป็นเว่ย — แพ้เพราะระบบกับตำแหน่ง ไม่ใช่เพราะน้อย"
> **ค้างข้อเดียวจากชุดทัก: หลอดทหาร HUD** (ทางเลือก ก "บรรทัดกำลังรบแนวตะวันตก" —
> งาน engine แยกเฟสหลัง LOCK บทที่ 7 · ดู LOG §5 เซสชันที่ 10 ข้อ 5 ทักข้อ 2–3) ·
> กติกาเดิมที่ยังใช้: **DECISIONS §12 ขั้น 1.5 บัญชีทัพ + 1.6 beat ตามเหตุการณ์ ·
> §19 เลย์เอาต์ป้าย** · โหมดสองเอกภพอยู่ที่ **c7-13** แล้ว (`shot.ps1 c7-13 -Mirror`) ·
> server ใช้พอร์ต 8779 (`tk3b`) — 8778 มักถูก session อื่นถือ
>
> เขียน 2026-08-25 · หลังปิดเฟส 0–2 และบทที่ 1 · **อัปเดตเซสชันที่ 2 (วันเดียวกัน):**
> งานค้างสามเรื่องก่อนบทที่ 2 ปิดหมดแล้ว — ① ชื่อทั้งโปรเจกต์เป็น**คำอ่านไทย** (DECISIONS §11
> เคาะใหม่ · เขียนฉากต้องหยิบชื่อจากตาราง `names.js`/`places.js` เท่านั้น ห้ามถอดเสียงเอง)
> ② การเติมสีใช้**เส้นขอบร่วม** (§16 · แก้รูปเขตแล้วต้องรัน `build_geo.js` เหมือนเดิม)
> ③ วงเส้นประมี**บรรทัดฐาน** (§17 · engine จัดการเองหมด ไม่ต้องประกาศอะไรต่อฉาก)
> และ `shot.ps1` แก้บั๊กภาพไม่มีวง/ป้าย (BUGS_SEEN §E7) แล้ว
> **อัปเดตเซสชันที่ 3 (จบ 2026-08-26):** ④ สีทาบเส้นประเขตแดนที่พิมพ์บนแผ่นสนิทแล้ว
>    (bordermask + seed clip — §16 · เมืองย้ายเขตที่ยอมรับ: weinan, lueyang)
> ⑤ builder เส้นทาง "สุ่มจุด+ลบมุม" เป็นบรรทัดฐาน (§4) · placeholder เป็นเส้นตรงต่อจุด
> ⑥ **ดีไซน์เคาะครบ:** วงเน้น = ก ฉบับเกลา (§17) · ลูกศร = ข (§3) —
>    สวิตช์ทดลองกับ `_variants.html` ลบแล้ว ห้ามขุดแบบที่แพ้กลับมา
> ⑦ (2026-08-26) **กำแพงเมืองจีนเป็นแนวกั้นสีแล้ว** (§16 — เจ้าของวงแอ่งหลงโย่วยืนยัน
>    "อิงของจริง") · มุม SW เป็นสีจ๊กถึงเส้นแดง · **beiyuan ค้นแล้วเสนอ (612,608)
>    chk คงไว้รอเจ้าของพยักหน้า** (LOG §7.2) — งานเคาะอื่นปิดหมด เหลือข้อเดียวนี้ก่อนบทที่ 2
> **อ่านไฟล์นี้ก่อน แล้วค่อยอ่าน `DECISIONS.md` และ `BUGS_SEEN.md`**

---

## 0. สถานะ ณ วันส่งต่อ

| เฟส | สถานะ |
|---|---|
| 0 · เนื้อเรื่อง | ✅ **ปิดแล้ว** · 11 ภาค · ค.ศ. 221–274 · `What_If_Zhao_Yun_Remonstrance.md` = source of truth **ห้ามแก้** |
| 1 · กฎ + เครื่องมือ | ✅ **ปิดแล้ว** · `DECISIONS.md` + `BUGS_SEEN.md` ไฟล์ละหนึ่ง · กราฟถนนแทนการวาดเส้น |
| 2 · กระดาน | ✅ **ปิดแล้ว** · 33 เขต · 100 หมุด · 41 node · 48 edge · ตัวเลขตั้งต้นปี 221 ล็อกแล้ว |
| 3 · เขียนทีละบท | 🔄 **บทที่ 0–9 LOCK (118 beat ถึงปี 262)** · บทที่ 10 รอเคาะ LOCK · เหลือบทที่ 11–12 |
| 4 · รวมเล่ม | ⬜ ยังไม่เริ่ม |

**ตัวตรวจผ่านครบ 5/5** · Node.js v24.19.0 ลงแล้ว

```bash
node tools/build_timeline.js --wip    # ⚠ ห้ามตัด output (BUGS_SEEN §C1)
node tools/check_routes.js
node tools/check_map.js
node tools/check_camera.js
node tools/check_fact.js
node tools/check_hud.js
node tools/board_preview.js --embed   # ส่องกระดานด้วยตาโดยไม่ต้องมี timeline
tools\shot.ps1 c1-06                  # ถ่ายภาพแอปจริง
```

---

## 1. สิ่งที่ต่างจากสองโปรเจกต์ก่อนแบบที่จะกัดถ้าไม่รู้

### 1.1 ★★ เส้นทางเดินทัพเป็น **กราฟ** ไม่ใช่เส้นที่วาดมือ

`data/roads.js` = node + edge + marches · **ประกาศการเดินทัพเป็นรายชื่อจุด** แล้ว builder
สร้าง `TK.routes` ให้ engine ในรูปแบบเดิม — `strategic.js` ไม่ต้องแก้เลยสักบรรทัด

```js
liubei_chengdu_hanzhong_221: {
  side:"han", commander:"Liu Bei", troops:45000,
  path:["chengdu","fucheng","zitong","baishui","yangping","hanzhong"]
}
```

**กฎที่ถูกยกเลิกทั้งกอง:** ห้ามเส้นตรง/คด 3% · `flips`/`maxTurn` · ตาราง `STRAIGHT_OK` ·
ตาราง `SHAPE_OK` · ตาราง `ENDS` · ธง `reverse` — **อย่าขุดกลับมา** เหตุผลอยู่ใน DECISIONS §4

**`d` เป็น optional** ถ้ายังไม่ลากถนนตามภูมิประเทศ builder ใส่เส้นโค้งชั่วคราวให้ แล้ว
`check_routes` รายงานเป็น**งานค้าง ไม่ใช่ error** · ตอนนี้ลากแล้ว 6 จาก 48 เส้น

### 1.2 ★ `roads.js` ห้ามประกาศพิกัดซ้ำกับ `places.js`

node ที่ไม่มี x/y จะไปดึงจาก `places.js` ด้วย id ที่ตรงกัน · **หาไม่เจอ = throw ตอนโหลด**
ใส่พิกัดตรงใน `roads.js` ได้เฉพาะจุดที่ไม่ใช่สถานที่ในเรื่อง (ตอนนี้มีอันเดียว: `wugong_ford`)

⚠ **ลำดับโหลดสำคัญ** — `places.js` ต้องมาก่อน `roads.js` เสมอ ทั้งใน `index.html` และในทุกตัวตรวจ

### 1.3 `var TK = window.TK;` ที่หัว `roads.js` เป็นของจำเป็น

`check_routes.js` ตั้ง `global.window = global` แต่ `build_timeline.js` ตั้ง `global.window = {}`
ไฟล์ data ที่ใช้ `TK` เปล่า ๆ จะพังกับตัวหลัง · ผูกชื่อไว้ที่หัวไฟล์ครั้งเดียวจบ

### 1.4 ภาคสามกับภาคสี่ **เล่าซ้อนเวลากันโดยตั้งใจ** (224–225 ทั้งคู่)

ภาคสามเล่าฝั่งฮั่น ภาคสี่เล่าสองปีเดียวกันจากฝั่งวุ่ย · กฎ "ปีต้องเดินหน้า" ของ build
เป็นกฎต่อบท ไม่ใช่ข้ามบท จึงผ่านอยู่แล้ว — **แต่คนที่มาอ่านทีหลังจะนึกว่าแถบเวลาพัง**

### 1.5 ตัวเลขตั้งต้นคำนวณใหม่ทั้งชุด **ห้ามลอกจากโปรเจกต์ก่อน**

```
ปี 221   ฮั่น 1.03 ล้าน / 12.5 หมื่น
         วุ่ย 4.73 ล้าน / 43.6 หมื่น
         ง่อ  2.07 ล้าน / 18.5 หมื่น
ปี 226   ฮั่น 1.44 ล้าน  ← ตรงกับ "คนล้านครึ่ง" ที่ Liu Bei พูดข้างเตียง
ปี 262   เหนือที่กลืน 4.32 ล้าน
```

**ง่อใหญ่กว่าโปรเจกต์ 2** เพราะถือ Jing Zhou ทั้งผืนตั้งแต่ปี 220
**ฮั่นจะไม่มีวันตกฮวบในปี 222** เพราะไม่มี Yiling ให้หัก — ถ้าเห็นกำลังฮั่นดิ่งช่วงนั้น
แปลว่ามีคนเผลอใส่ `losses` ผิดที่

---

## 2. ทุ่นระเบิดที่ session นี้เหยียบมา (ไม่มีใน BUGS_SEEN ตอนเริ่ม)

### 2.1 ★ `joinD` แทน `M` ด้วย `L` แล้วพังเงียบ
ต่อ path หลายช่วงโดยแทนคำสั่ง `M` ด้วย `L` → ได้ `L Q 362,658` ที่ไม่มีพิกัดตาม L
**เบราว์เซอร์หยุด parse เงียบ ๆ เส้นสามช่วง 370 ลี้เหลือ 27px โดยไม่มี error สักตัว**
→ ต้อง **ตัดคำสั่ง M ทิ้งทั้งคำสั่ง** ไม่ใช่แทนที่

### 2.2 ★★ landmask ทำเครื่องหมายแม่น้ำเป็นน้ำ และเมืองโบราณตั้งริมแม่น้ำ
บันทึกเต็มใน `BUGS_SEEN` §A5 · **อย่าเขียนตัวตรวจที่ถามว่า "จุดอยู่บนบกไหม"**
ให้ถามว่า "ลอยกลางผืนน้ำไหม" กับ "เปียกติดต่อกันยาวเกินแม่น้ำไหม"

### 2.3 `build_geo` ตัดสินการทับด้วย**ลำดับการประกาศ**
เขียน `guanzhong` ก่อน `tongguan` แล้วหมุด Tong Pass ตกในกวานจง — ด่านหายจากแผนที่
ทั้งที่ยังมีเขตอยู่ · **เขตเล็กที่ซ้อนอยู่ในเขตใหญ่ ต้องประกาศก่อนเสมอ**

### 2.4 `who` บนลูกศรรับ **id ของคน** ไม่ใช่ป้าย
`who:"liubei"` ไม่ใช่ `who:"Liu Bei"` · build จะ error ให้ ซึ่งดี

### 2.5 ★ ผมแปล HUD แล้วตัดตัว "k" ทิ้ง — "130 คน" ที่แปลว่า 130,000
`troops` เก็บเป็นหน่วย**หมื่น** (万) ของเดิมคูณ 10 แล้วเติม k · ที่ถูกคือคูณ 10,000
**ทุกครั้งที่แตะตัวเลขบน UI ให้ถ่ายภาพดู** ตัวตรวจไม่เห็นหน่วยที่หายไป

### 2.6 กรอบปิดทับ WEI/SHU/WU ที่ opacity .92 ทะลุมา 8%
วางตำแหน่งถูกมาตลอดสองโปรเจกต์ แต่พอกล้องซูมเข้า 2–3 เท่า (เล่มนี้มีฉากใกล้เยอะ)
ตัวอักษรกลับมาอ่านออก และตัวที่แรงที่สุดคือ "WEI" ที่ลอยอยู่บนกวานจง
→ ปรับเป็น `.985` แล้ว

### 2.7 `shot.ps1` เขียนภาพลง `%TEMP%\tk-shots` ซึ่ง**ใช้ร่วมกับโปรเจกต์เก่า**
ในโฟลเดอร์นั้นมี `c1-01.png` `c1-04.png` ฯลฯ ของเล่มอื่นปนอยู่
**ดูวันที่ไฟล์เสมอก่อนเชื่อว่าเป็นภาพของเล่มนี้**

### 2.8 PowerShell 5.1 รายงาน stderr ของ Chrome เป็น error
`shot.ps1 a,b` จะหยุดหลังภาพแรก · ยิงทีละ beat หรือตั้ง `$ErrorActionPreference='SilentlyContinue'`

---

## 3. บทที่ 1 เขียนยังไง (ใช้เป็นแบบสำหรับบทถัดไป)

11 ฉาก · `p0-01`, `p0-02`, `c1-01` … `c1-09` · **ไม่มี `mapDelta` เลยทั้งบท** ซึ่งถูกต้อง —
ปี 221 ไม่มีแผ่นดินเปลี่ยนมือ แผ่นแรกจะเปลี่ยนสีที่ Xiabian ในบทที่ 2

**การตัดสินใจสองข้อที่ควรทำซ้ำ:**

1. **ไม่มีลูกศรของ Deng Zhi ไปง่อ** ทั้งที่เป็นราชโองการที่สำคัญที่สุด — ทูตคนเดียว
   ไม่ได้เดินทัพ และ**ลูกศรอ้างการไปถึงด้วยกำลัง** · ผลของภารกิจแสดงเป็น**หมุดง่อ 50,000
   ที่ Jiangling** แทน ซึ่งคือสิ่งที่ราชโองการฉบับนั้นซื้อมาจริง ๆ
2. **ไม่ปักหมุด Dangyang ในฉากศาล** ทั้งที่ข้อความเอ่ยถึง — กล้องที่คลุมทั้งสองที่ต้องกว้าง
   720+ ซึ่งทิ้งความรู้สึกของฉากไปหมด · `check_camera` จับให้ตอน build แรก

**ตัวเลข `strength` ของ c1-06 ตั้งเอง (45,000)** ต้นฉบับไม่ได้ให้ไว้ · รอบแรกตั้ง 60,000
แล้ว `check_hud` ฟ้องว่าเป็น 46% ของทั้งรัฐ · ~~45,000 = 35% พอดี~~ **แก้ 2026-08-27:
จริง ๆ คือ 36%** — "35% พอดี" เป็นผลของการปัดตัวหารเป็น 130,000 ตัวหารจริงคือ 125,000
พอเปลี่ยนไปปัดที่หลักพัน ข้อ 5 ก็ฟ้องทันที · ลง `SHARE_OK` แล้วพร้อมเหตุผล (ราชโองการ
ฉบับแรกของรัชกาล = การทุ่มสุดตัวครั้งเดียวที่กติกาเจาะช่องไว้ให้) · 45,000 ยังตรงกับกำลังที่
Liu Bei ยกไป Yiling จริงในเอกภพของเรามากกว่า

---

## 4. งานถัดไป — บทที่ 2 (ภาคสอง · 221–222)

**ต้องเตรียมก่อนเขียนฉาก:**

| ของที่ต้องมี | หมายเหตุ |
|---|---|
| `mapDelta` ก้อนแรกของเรื่อง | `wudu` → han (ฤดูใบไม้ร่วง 221) |
| `mapDelta` ก้อนที่สอง | `longxi` · `tianshui` · `anding` → han (ปี 222 ฤดูเดียว) |
| march ของ Ma Chao ขึ้นที่ราบสูง Qiang | ยังไม่มี node ทางเหนือของ Longyou — ต้องเพิ่ม |
| march เข้าเมือง Ji Cheng | มี edge `qishan–jicheng` แล้ว (ลากจริงแล้ว) |
| `clash` ที่ Xiabian | ศึกจริงศึกแรกของเรื่อง — ต้องมี `losses` |
| CLAIMS: ทหารม้า Qiang 12,000 | อยู่ในตารางแล้ว รอ id |

⚠ **Ma Chao ตายเดือน 8 ปี 222** — กติกาข้อแรก ห้ามเลื่อน

### 4.1 พิกัด 19 จุดที่ยังติดธง `chk:true`

```
jicheng · nanan · anding · yangxi · baohan · jiuquan · xiping · qilian · wugong
wugong_ford · beiyuan · xiaoguan · shanxian · jinyang · fanyang · hefei
changban · zhuojun · hanshou · gaoping
```

**ตัวที่ต้องแก้ก่อนใครเพื่อน: `beiyuan`** — โปรเจกต์ 1 ให้ (302,595) ซึ่งอยู่แถว Longxi
ห่างจาก Chang'an ไปตะวันตก 340px เป็นไปไม่ได้สำหรับ "ที่ราบฝั่งเหนือที่เป็นกุญแจของ Chang'an"
ผมใส่ค่าชั่วคราว (620,618) ไว้ · **ศึก Beiyuan ปี 233 คือไคลแมกซ์ของภาคเจ็ด ห้ามใช้ค่าเดา**

ใช้ `tools/georef.js` กับ `tools/calibrate.html` สอบเทียบ

### 4.2 ของที่ยังไม่ทำและรู้ตัวว่ายังไม่ทำ

- **ตัดคำไทยใน `labeler.js`** — ไทยไม่มีช่องว่างระหว่างคำ ตอนนี้ป้ายยาว ๆ ยังไม่ได้ทดสอบ
  การตัดบรรทัด · ฉากที่ผ่านมาป้ายสั้นพอเลยยังไม่เห็นปัญหา **จะเห็นตอนบทที่มีป้ายยาว**
- **ฟอนต์** — ยังใช้ system stack เดิม ไทย+ละตินเบสไลน์ยังไม่ได้ตรวจอย่างจริงจัง
- **โหมด visual ใหม่ทั้งสาม** (ถนน · เสบียง · สองเอกภพ) ยังไม่ได้เขียน
  ปุ่ม `#btnRoads` มีใน `index.html` แล้วแต่**ยังไม่มีโค้ดหลังปุ่ม**
- `check_click.js` · `selfcheck.js` ยังไม่ได้รันเลยสักครั้งในโปรเจกต์นี้

---

## 5. ไฟล์อ้างอิงเรียงตามลำดับที่ควรอ่าน

| ไฟล์ | คืออะไร |
|---|---|
| `docs/DECISIONS.md` | **กฎที่ล็อกแล้ว ไฟล์เดียว** 18 หมวด |
| `docs/BUGS_SEEN.md` | แผลเป็นหมวด A–E — **หมวด B ตัวตรวจมองไม่เห็น ต้องถ่ายภาพ · E7 ภาพโกหกเรื่องชั้น opacity** |
| `docs/STORY_REVIEW.md` | รีวิวเนื้อเรื่อง + การตัดสิน 8 ข้อที่ล็อกแล้ว |
| `docs/PROPOSAL_FLOW.md` | flow 5 เฟส และข้อเสนอกราฟถนนฉบับเต็ม |
| `docs/SAMPLE_NEW_SCENES.md` | ตัวอย่างที่ใช้เคาะน้ำเสียง เก็บเป็นหลักฐาน |
| `data/geo.js` (หัวไฟล์) | เหตุผลที่แตกภาคตะวันตกละเอียดขนาดนี้ |
| `data/roads.js` (หัวไฟล์) | สัญญาของกราฟถนนทั้งสามชั้น |
| `data/_part1.js` (หัวไฟล์) | แบบของการเขียนฉาก |
